name: catalogo_virtual
description: Aplicación de catálogo con 2 pantallas para examen (sin imágenes).
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/descripcion_damas.txt
    - assets/descripcion_caballeros.txt
